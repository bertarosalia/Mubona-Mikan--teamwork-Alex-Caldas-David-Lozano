import NotFoundPageStyled from "./NotFoundPageStyled";

const NotFoundPage = (): JSX.Element => {
  return (
    <NotFoundPageStyled>
      <h2 className="not-found-page">404 ERROR</h2>
    </NotFoundPageStyled>
  );
};

export default NotFoundPage;
